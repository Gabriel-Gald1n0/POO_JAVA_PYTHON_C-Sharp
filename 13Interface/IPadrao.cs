interface IPadrao
{
    // Interface é um contrato que obriga a classe que a implementa a ter os métodos que estão dentro dela
    // Não pode ter corpo
    void somar(int n1, int n2);
    void subtrair(int n1, int n2);
}